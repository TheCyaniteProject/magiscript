{
  "name": "magiscript",
  "version": "1.0.0",
  "description": "A small Electron fantasy canvas prototype",
  "main": "main.js",
  "bin": {
    "magi": "./cli.js"
  },
  "scripts": {
    "start": "electron .",
    "debug": "electron . -- --debug",
    "gui-test": "electron . -- --test-demo",
    "test": "node --test --test-concurrency=1 test/*.test.js"
  },
  "devDependencies": {
    "electron": "^35.0.0"
  }
}
